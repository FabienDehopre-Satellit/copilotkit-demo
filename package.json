{
  "name": "copilotkit-demo",
  "private": true,
  "packageManager": "pnpm@11.6.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "pnpm run -r --parallel --if-present dev"
  },
  "devDependencies": {
    "prettier": "catalog:"
  }
}
