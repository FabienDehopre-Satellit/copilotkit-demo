# Copy to `.env` at the repo root. Both tiers load this file explicitly — the runtime with
# `process.loadEnvFile('../.env')`, the phase-2 agent with `Env.Load("../.env")`. Do not
# export it in your shell instead.
OPENAI_API_KEY=sk-...
